name: Issue 19 skipped proof

on:
  pull_request:

permissions:
  contents: read

jobs:
  skipped:
    name: Skipped proof
    runs-on: ubuntu-24.04
    if: ${{ false }}
    steps:
      - run: echo "This job should be skipped"
