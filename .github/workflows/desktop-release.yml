name: Desktop Release

# Builds the Electron desktop app (desktop/) into self-contained installers for
# Linux (AppImage/deb/rpm), macOS (dmg), and Windows (nsis). Each installer
# bundles Chromium + Node + the Rust sidecars (archcar, archductor), so target
# machines need no Archductor-specific runtime libraries.
#
# Runs alongside the GTK publish workflow during the transition; retire that one
# once the Electron app reaches parity.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version without leading v, for example 0.1.0"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: desktop-release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # Derive + validate the release version once, so every downstream job agrees.
  version:
    name: Resolve version
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      version: ${{ steps.derive.outputs.version }}
      tag: ${{ steps.derive.outputs.tag }}
    steps:
      - name: Derive version
        id: derive
        env:
          EVENT_NAME: ${{ github.event_name }}
          VERSION_INPUT: ${{ inputs.version }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            VERSION="$VERSION_INPUT"
          else
            VERSION="${REF_NAME#v}"
          fi
          # npm-compatible SemVer: MAJOR.MINOR.PATCH with optional -prerelease
          # and +build metadata. Rejects malformed values like 1.2.3.foo.
          semver='^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?$'
          if ! grep -Eq "$semver" <<< "$VERSION"; then
            echo "version must be npm-compatible SemVer MAJOR.MINOR.PATCH, got: $VERSION" >&2
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

  # Gate packaging on the tests + smokes. Packaging cannot start until this
  # passes (see `needs` on the package job).
  verify:
    name: Verify
    needs: version
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 11

      # Rust tests for the crates the desktop UI depends on (archcar RPCs + CLI),
      # plus a CLI smoke of the read RPCs. The full GTK workspace suite runs in
      # ci.yml; the GTK app is being retired.
      - name: Rust tests (core + cli)
        run: cargo test --locked -p archductor-core -p archductor

      - name: Build sidecars for smoke
        run: cargo build --release --locked --bin archcar --bin archductor

      - name: CLI smoke (archcar read RPCs)
        run: |
          set -euo pipefail
          export PATH="$PWD/target/release:$PATH"
          ./target/release/archductor archcar repositories
          ./target/release/archductor archcar workspaces

      - name: Desktop install
        working-directory: desktop
        run: pnpm install --frozen-lockfile

      - name: Desktop typecheck + unit tests + build
        working-directory: desktop
        run: |
          pnpm typecheck
          pnpm test
          pnpm build

  # Create or reuse the GitHub release the installers attach to, for BOTH tag
  # pushes and manual dispatch, before any upload runs.
  release:
    name: Ensure release
    needs: [version, verify]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Create or reuse release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ needs.version.outputs.tag }}
          TARGET_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "release $TAG already exists — reusing"
          else
            gh release create "$TAG" --title "$TAG" --generate-notes --target "$TARGET_SHA"
          fi

  package:
    name: Package (${{ matrix.os }})
    needs: [version, verify, release]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 90 # notarization can add 5-30 min on the mac leg
    permissions:
      contents: write
    # Every run step here is written in POSIX shell. Without this the Windows
    # runner executes them in PowerShell, where `$VERSION` names a PowerShell
    # variable rather than the environment, so `npm version "$VERSION"` ran as
    # `npm version ""` and failed with "The variable '$VERSION' cannot be
    # retrieved because it has not been set" — and `set -euo pipefail` in the
    # packaging step would have failed straight after it.
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            platform: linux
          - os: macos-14
            platform: mac
          - os: windows-2025
            platform: win
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable

      - name: Linux packaging prerequisites
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -q
          # rpm -> rpmbuild for the rpm target; fuse for AppImage.
          sudo apt-get install -y --no-install-recommends rpm fuse libfuse2 git

      - name: Build Rust sidecars (release)
        env:
          # Crate versions are not bumped per release; stamp the tag so the
          # bundled CLI reports the same version as the app around it.
          ARCHDUCTOR_RELEASE_VERSION: ${{ needs.version.outputs.version }}
        run: cargo build --release --locked --bin archcar --bin archductor

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 11

      - name: Set package version
        working-directory: desktop
        env:
          VERSION: ${{ needs.version.outputs.version }}
        run: npm version "$VERSION" --no-git-tag-version --allow-same-version

      - name: Install desktop deps
        working-directory: desktop
        run: pnpm install --frozen-lockfile

      # Kept separate from packaging so the signing secrets are never in the
      # environment of the Vite build and the npm dependency tree it executes.
      - name: Build desktop bundle
        working-directory: desktop
        run: pnpm build

      - name: Package installers
        working-directory: desktop
        # No GH_TOKEN here so electron-builder never auto-publishes; the upload
        # step below attaches artifacts explicitly.
        #
        # The mac build signs with the Developer ID Application identity
        # (CSC_LINK is the base64 .p12 — electron-builder reads it straight
        # from the env var) and notarizes via notarytool with the Apple ID
        # credentials. Squirrel.Mac requires both before it will self-update.
        # The env vars are empty strings on the other platforms, which
        # electron-builder must not see as "a cert was provided" — hence the
        # conditional exports below.
        env:
          EB_PLATFORM: ${{ matrix.platform }}
          MACOS_CERT_P12_BASE64: ${{ secrets.MACOS_CERT_P12_BASE64 }}
          MACOS_CERT_PASSWORD: ${{ secrets.MACOS_CERT_PASSWORD }}
          MACOS_APPLE_ID: ${{ secrets.APPLE_ID }}
          MACOS_APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          MACOS_APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          if [ "$EB_PLATFORM" = "mac" ]; then
            : "${MACOS_CERT_P12_BASE64:?mac release needs the MACOS_CERT_P12_BASE64 secret}"
            : "${MACOS_APPLE_ID:?mac release needs the APPLE_ID secret}"
            export CSC_LINK="$MACOS_CERT_P12_BASE64"
            export CSC_KEY_PASSWORD="$MACOS_CERT_PASSWORD"
            export APPLE_ID="$MACOS_APPLE_ID"
            export APPLE_APP_SPECIFIC_PASSWORD="$MACOS_APPLE_APP_SPECIFIC_PASSWORD"
            export APPLE_TEAM_ID="$MACOS_APPLE_TEAM_ID"
          fi
          pnpm exec electron-builder "--$EB_PLATFORM" --publish never

      - name: Collect artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: desktop-${{ matrix.os }}
          path: |
            desktop/release/*.AppImage
            desktop/release/*.deb
            desktop/release/*.rpm
            desktop/release/*.dmg
            desktop/release/*.zip
            desktop/release/*.exe
            desktop/release/latest*.yml
            desktop/release/*.blockmap
          if-no-files-found: warn

      - name: Attach to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ needs.version.outputs.tag }}
          MATRIX_OS: ${{ matrix.os }}
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          # latest*.yml and the blockmaps are the update feed the in-app
          # updater reads; without them on the release, a packaged app can see
          # no new version even though the installers are right there. The mac
          # zip is what Squirrel.Mac actually installs from (dmg = first
          # install only), so latest-mac.yml points at it.
          files=(desktop/release/*.AppImage desktop/release/*.deb desktop/release/*.rpm desktop/release/*.dmg desktop/release/*.zip desktop/release/*.exe desktop/release/latest*.yml desktop/release/*.blockmap)
          if [ ${#files[@]} -eq 0 ]; then
            echo "no installers produced on $MATRIX_OS" >&2
            exit 0
          fi
          gh release upload "$TAG" "${files[@]}" --clobber
