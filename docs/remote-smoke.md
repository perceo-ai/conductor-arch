# Remote smoke test

Created from a macOS laptop driving an archductor daemon on a Proxmox host over
Tailscale. The worktree, the commit, and the pull request all execute
server-side; the laptop only speaks the archcar protocol.