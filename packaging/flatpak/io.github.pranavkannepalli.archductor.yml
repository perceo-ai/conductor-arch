app-id: io.github.pranavkannepalli.archductor
runtime: org.gnome.Platform
runtime-version: '47'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable

command: archductor-gtk

finish-args:
  # Full filesystem access required for arbitrary repository and workspace paths
  - --filesystem=host
  # Network access for git fetch, gh CLI, and agent sessions
  - --share=network
  # IPC for desktop integration
  - --share=ipc
  # Allow launching editors and terminal emulators on the host
  - --socket=fallback-x11
  - --socket=wayland
  # Talk to the host shell environment
  - --env=SHELL=/bin/bash

build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
  env:
    CARGO_HOME: /run/build/archductor/cargo
    RUST_BACKTRACE: '1'

modules:
  - name: archductor
    buildsystem: simple
    build-commands:
      - cargo build --release --locked --workspace
      - install -Dm755 target/release/archductor /app/bin/archductor
      - install -Dm755 target/release/archductor-gtk /app/bin/archductor-gtk
      - install -Dm644 README.md /app/share/doc/archductor/README.md
      - install -Dm644 packaging/flatpak/io.github.pranavkannepalli.archductor.appdata.xml
          /app/share/metainfo/io.github.pranavkannepalli.archductor.appdata.xml
      - install -Dm644 packaging/flatpak/io.github.pranavkannepalli.archductor.desktop
          /app/share/applications/io.github.pranavkannepalli.archductor.desktop
      - install -Dm644 packaging/archductor.svg
          /app/share/icons/hicolor/scalable/apps/io.github.pranavkannepalli.archductor.svg
    sources:
      - type: dir
        path: ../..
      - type: shell
        commands:
          - cargo vendor --locked vendor
    build-options:
      env:
        CARGO_NET_OFFLINE: 'true'
      config-opts:
        - --replace-deps-source=vendor
