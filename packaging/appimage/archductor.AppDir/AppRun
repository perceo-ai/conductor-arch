#!/bin/bash
# AppRun — entry point for the archductor AppImage.
# The AppImage runtime sets APPDIR to the squashfs mount point.
# Runs the archductor CLI, passing through any arguments.
set -euo pipefail
SELF_DIR="$(dirname "$(readlink -f "$0")")"
exec "$SELF_DIR/usr/bin/archductor" "$@"
